# deps
node_modules/

# OS / editor
.DS_Store
*.log
Thumbs.db

# datos crudos y sensibles — NUNCA al repo
# (los exports de MeLi/TN tienen ventas y datos de compradores)
*.xlsx
*.xls
*.csv
Tablero Buyer Naku/

# credenciales / config local
.env
.env.*
appsscript/.clasp.json
appsscript/.clasprc.json

# temporales
*.tmp
scratchpad/
.backup/

# credenciales del puente de Apps Script (url + token)
.naku-puente.json

# El JSON del tablero de dirección: tiene el estado de resultados y el repo es
# público. Vive acá y se sube a la base, que lo sirve con clave.
# (docs/data/lines.json es otra cosa: lo necesita el tablero de ventas de Leo.)
datos/
